import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  header: {fontWeight: 'bold', fontSize: 25, textTransform: 'uppercase'},
  counter: {fontWeight: 'bold', fontSize: 25},
});

export default styles;
