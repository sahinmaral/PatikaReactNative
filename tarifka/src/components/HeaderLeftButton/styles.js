import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  text: {
    color: '#f97316',
    fontWeight: '400',
    fontSize: 20,
    marginRight: 30,
  }
});

export default styles;
